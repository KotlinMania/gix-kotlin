# Port targets — `gix-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `gix` that codex-rs uses, and therefore the minimum subset that `gix-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-56] (rank `130`, tier `direct`, status `To Do`)

**Cargo package name**: `gix`
**Rust module name (in `use` statements)**: `gix`

## Summary

- **5** distinct symbols imported by codex-rs
- **5** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `gix::hash::ObjectId`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-git-utils`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/baseline.rs:2` (`codex-git-utils`) — `use gix::hash::ObjectId`

### `gix::objs::Tree`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-git-utils`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/baseline.rs:3` (`codex-git-utils`) — `use gix::objs::Tree`

### `gix::objs::tree::Entry`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-git-utils`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/baseline.rs:4` (`codex-git-utils`) — `use gix::objs::tree::Entry`

### `gix::objs::tree::EntryKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-git-utils`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/baseline.rs:5` (`codex-git-utils`) — `use gix::objs::tree::EntryKind`

### `gix::objs::tree::EntryMode`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-git-utils`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/baseline.rs:6` (`codex-git-utils`) — `use gix::objs::tree::EntryMode`
